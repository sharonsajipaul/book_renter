export default function NavBar() {
    return (
        <nav>
            <a href="home.html">Home</a>
            <a href="#">Shop</a>
            <a href="#">About Us</a>
            <a href="login.html">Login</a>
        </nav>
    );
}
